const API='https://autoremate-api.jonagar90.workers.dev',WA='50233584071';let cars=[];
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const img=k=>!k?'':k.startsWith('http')?k:`${API}/${k.replace(/^\/+/,'')}`,money=n=>Number(n)>0?'Q '+Number(n).toLocaleString('en-US'):'';
const title=c=>c.title||[c.year,c.make,c.model,c.trim].filter(Boolean).join(' ')||'Deal AutoRemate',active=c=>c.status!=='vendido'&&c.type!=='vendido',stat=c=>c.status==='disponible'?'DISPONIBLE':'EN TRÁNSITO';
function card(c,sold=false){const im=(c.images||[])[0],sp=[c.fuel,c.transmission,c.mileage?Number(c.mileage).toLocaleString()+' millas':''].filter(Boolean).join(' · ');return `<article class="dealcard" onclick="openDeal('${esc(c.id)}')"><div class="dealimg">${im?`<img src="${esc(img(im))}" alt="${esc(title(c))}" loading="lazy">`:`<div class="noimg">AutoRemate</div>`}<span class="badge ${sold?'sold':''}">${sold?'VENDIDO':esc(stat(c))}</span></div><div class="dealbody"><h3>${esc(title(c))}</h3>${sp?`<p class="spec">${esc(sp)}</p>`:''}${!sold&&money(c.price)?`<div class="price">${esc(money(c.price))}</div>`:''}<button class="linkbtn">${sold?'VER VEHÍCULO':'VER DEAL →'}</button></div></article>`}
async function load(){try{const r=await fetch(API+'/api/cars');if(!r.ok)throw 0;const d=await r.json();cars=Array.isArray(d)?d:Array.isArray(d.cars)?d.cars:[];render();openFromUrl()}catch(e){document.querySelectorAll('[data-cars]').forEach(x=>x.innerHTML='<div class="empty">No pudimos cargar los Deals.</div>')}}
function render(){const a=cars.filter(active),s=cars.filter(c=>!active(c)),ac=document.querySelector('[data-cars="active"]'),sc=document.querySelector('[data-cars="sold"]');if(ac){ac.classList.add('dealgrid');ac.innerHTML=a.map(c=>card(c)).join('')||'<div class="empty">No hay Deals disponibles.</div>'}if(sc){sc.classList.add('dealgrid','soldgrid');sc.innerHTML=s.map(c=>card(c,true)).join('')||'<div class="empty">Todavía no hay Deals vendidos.</div>'}}
function openDeal(id){const c=cars.find(x=>String(x.id)===String(id));if(!c)return;const ims=c.images||[],facts=[['Estado',active(c)?stat(c):'VENDIDO'],['Llegada estimada',c.arrival||''],['Stock',c.stock||''],['Millaje',c.mileage?Number(c.mileage).toLocaleString()+' millas':''],['Transmisión',c.transmission||''],['Combustible',c.fuel||''],['Tracción',c.drivetrain||''],['Color',c.color||''],['Tipo de título',c.titleType||''],['Condición / daño',c.damage||'']].filter(x=>x[1]),u=`${location.origin}${location.pathname}?deal=${encodeURIComponent(c.id)}`,wt=[`Hola AutoRemate, me interesa el Deal: ${title(c)}.`,c.stock?`Stock: ${c.stock}.`:'',`Link: ${u}`].filter(Boolean).join(' '),g=ims.length?`<div class="gallery"><div class="mainphoto"><img id="mainDealPhoto" src="${esc(img(ims[0]))}"></div><div class="thumbs">${ims.map((x,i)=>`<button class="thumb ${i===0?'active':''}" onclick="changeDealPhoto('${esc(img(x))}',this)"><img src="${esc(img(x))}"></button>`).join('')}</div></div>`:`<div class="gallery"><div class="mainphoto noimg">AutoRemate</div></div>`,f=`<div class="facts">${facts.map(([l,v])=>`<div class="fact"><span>${esc(l)}</span><strong>${esc(v)}</strong></div>`).join('')}</div>`,p=active(c)&&money(c.price)?`<div class="modalprice">${esc(money(c.price))}</div>`:'',w=active(c)?`<a class="btn green modalwa" href="https://wa.me/${WA}?text=${encodeURIComponent(wt)}" target="_blank">WhatsApp · +502 3358 4071</a>`:'';document.getElementById('modalContent').innerHTML=`<div class="modalgrid">${g}<div class="modalinfo"><div class="modalstatus">${active(c)?esc(stat(c)):'VENDIDO'}</div><h2>${esc(title(c))}</h2>${p}${f}<p class="modalnote">Fotografías reales del vehículo. Consulta las condiciones de compra por WhatsApp.</p>${w}</div></div>`;document.getElementById('dealModal').classList.add('open');document.body.classList.add('modalopen');history.pushState({},'',`${location.pathname}?deal=${encodeURIComponent(c.id)}`)}
function changeDealPhoto(src,b){const m=document.getElementById('mainDealPhoto');if(m)m.src=src;document.querySelectorAll('.thumb').forEach(x=>x.classList.remove('active'));if(b)b.classList.add('active')}
function closeDeal(){document.getElementById('dealModal').classList.remove('open');document.body.classList.remove('modalopen');history.pushState({},'',location.pathname)}
function openFromUrl(){const id=new URLSearchParams(location.search).get('deal');if(id)openDeal(id)}
document.addEventListener('DOMContentLoaded',()=>{load();document.getElementById('closeModal').onclick=closeDeal;document.getElementById('dealModal').onclick=e=>{if(e.target.id==='dealModal')closeDeal()};document.addEventListener('keydown',e=>{if(e.key==='Escape')closeDeal()})});
